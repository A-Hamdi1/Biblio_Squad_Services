name: biblio_squad
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0


dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.6
  firebase_core: ^3.13.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.4
  ocr_service:
    path: modules/ocr_service
  translation_service:
    path: modules/translation_service
  barcode_service:
    path: modules/barcode_service
  document_scan_service:
    path: modules/document_scan_service

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
